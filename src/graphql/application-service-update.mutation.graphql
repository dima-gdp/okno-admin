mutation UpdateApplicationService($input: UpdateApplicationServiceInput!)  {
    updateApplicationService(input: $input) {
        id
        application_id
        is_selected
        name
        description
        cost
        sort
    }
}
